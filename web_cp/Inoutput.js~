document.getElementById("input").onsubmit
await function Conversion() {
    let inform = document.getElementById("input").content.value;
}
function write_Trans() {
    const into = document.getElementById("into");
    const question = document.getElementById("question");
    into.addEventListener("into",submmit){
	question.value = into.value;
    }
  }
